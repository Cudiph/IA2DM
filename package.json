{
  "name": "integrated-aria2-download-manager",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "export NODE_ENV=dev || set NODE_ENV=dev&& npm run build:manifest && vite build",
    "build": "vite build && npm run build:manifest",
    "build:manifest": "node scripts/build.cjs",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "@types/webextension-polyfill": "^0.10.7",
    "prettier": "^3.2.5",
    "prettier-plugin-svelte": "^3.2.3",
    "svelte": "^4.2.17",
    "vite": "^5.2.11",
    "webextension-polyfill": "^0.11.0"
  }
}
